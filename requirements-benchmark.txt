# HDH-Bench Requirements
# Install with: pip install -r requirements-benchmark.txt

# Core dependencies (used by scorer)
# No external dependencies required - uses Python stdlib

# Optional: For running agent scripts
# Uncomment as needed based on your agent implementation
# requests>=2.28.0
# httpx>=0.24.0
# anthropic>=0.7.0
# openai>=1.0.0
